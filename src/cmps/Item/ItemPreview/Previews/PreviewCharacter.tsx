import { CharacterModel } from "@/models/Character.model";

interface Props {
  character: CharacterModel;
}

export default function PreviewCharacter({ character }: Props) {

    return(<div></div>)
}
